import {Flex} from 'components/layout';
import wrapper from 'hoc/wrapper';

const DiscoverProfessionalsScreen = wrapper(
  function DiscoverProfessionalsScreen() {
    return <Flex />;
  },
);

export default DiscoverProfessionalsScreen;
